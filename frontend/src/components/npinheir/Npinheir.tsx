import Navbar from "./Navbar"
import { useState } from "react"

type Props = {}

const Npinheir = (props: Props) => {

  return (
    <div className="app bg-gradient-to-tl from-violet-900 via-violet-900 to-black">
      <Navbar />
    </div>
  )
}

export default Npinheir