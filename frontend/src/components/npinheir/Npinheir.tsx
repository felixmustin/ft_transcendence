import Navbar from "./Navbar"
import { useState } from "react"

type Props = {}

const Npinheir = (props: Props) => {

  return (
    <div className="app bg-gradient-to-b from-violet-900 via-violet-500 to-blue-800">
      <Navbar />
    </div>
  )
}

export default Npinheir